import { ProductCardConfig } from "../models/product-card-config.model";

export const dashboardProductCardConfig: ProductCardConfig = {
    hasUrl: true,
    hasImage: true,
    hasTitle: true,
    hasPrice: false
}
